# Algo_Analyzer
An Algo Analyzer project is a tool that takes user input data and compares the performance of different sorting algorithms, specifically Merge Sort and Quick Sort. It processes the given input by applying both algorithms separately and measures the time
